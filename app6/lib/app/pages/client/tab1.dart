/* import 'package:flutter/material.dart';
// import 'placeholder_widget.dart';
// import 'card_widget.dart';
import 'package:app10/app/pages/home/home_page.dart';
import 'package:app10/app/pages/maps/maps_page.dart'; */

// import 'favourites_page.dart';
//import 'recents_page.dart';
//import 'card_page.dart';
//import 'database/database.dart';

/* 
class _HomeState2 extends State<Home2> {
  int _currentIndex = 0;
  final List<Widget> _children = [RecentsPage(), MapsPage(), HomePage()];

  void onTabTapped(int index) {
    setState(() {
      _currentIndex = index;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Emirates Dial'),
      ),
      body: _children[_currentIndex], // new
      bottomNavigationBar: BottomNavigationBar(
        onTap: onTabTapped, // new
        currentIndex: _currentIndex, // new
        items: [
          new BottomNavigationBarItem(
            icon: Icon(Icons.home),
            title: Text('Recent Calls'),
          ),
          new BottomNavigationBarItem(
            icon: Icon(Icons.restore),
            title: Text('Register Card'),
          ),
          new BottomNavigationBarItem(
              icon: Icon(Icons.bubble_chart), title: Text('More'))
        ],
      ),
    );
  }
} */